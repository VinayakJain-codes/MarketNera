const fs = require('fs'); let html = fs.readFileSync('extracted_main.txt', 'utf8'); html = html.replace(/class=/g, 'className=').replace(/<img([^>]*)>/g, '<img />').replace(/<input([^>]*)>/g, '<input />').replace(/<!--[\s\S]*?-->/g, ''); const jsx = import Navbar from '@/components/layout/Navbar';\nimport Footer from '@/components/layout/Footer';\n\nexport default function AboutPage() {\n  return (\n    <div className=\g-[#06091C] min-h-screen text-slate-200\>\n      <Navbar />\n      \n      <Footer />\n    </div>\n  );\n}\n; fs.writeFileSync('src/app/about/page.tsx', jsx);
